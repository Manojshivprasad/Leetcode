# Python 3

class Solution:
    def isPalindrome(self, s: str) -> bool:
        j = ''
        for i in s:
            if i.isalnum():
                j += i.lower()
        return(j == j[::-1])
________________________________________________________________
Only the letters are stored in j with lower case
Check j is palindrome or not
________________________________________________________________
