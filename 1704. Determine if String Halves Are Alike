# Python 3

class Solution:
    def halvesAreAlike(self, s: str) -> bool:
        return(sum([1 for i in (s[:len(s)//2]) if i in 'AEIOUaeiou']) == sum([1 for j in (s[len(s)//2:]) if j in 'AEIOUaeiou']))
