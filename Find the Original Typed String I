# Python 3

class Solution:
    def possibleStringCount(self, word: str) -> int:
        l1 = list(word)
        c = 1
        for i in range(1,len(word)):
            if l1[i-1] == l1[i]:
                c += 1
        return(c)
