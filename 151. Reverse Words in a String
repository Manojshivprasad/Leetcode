#Python 3

class Solution:
    def reverseWords(self, s: str) -> str:
        s = s.strip(' ')
        x = s.split()
        str1 = ''
        for i in range(1,len(x)+1):
            str1 += x[-i] + ' '
        return(str1[:len(str1)-1])
