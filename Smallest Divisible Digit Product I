# Python 3

class Solution:
    def smallestNumber(self, n: int, t: int) -> int:
        while True:
            x = 1
            for i in str(n):
                x *= int(i)
            if x % t == 0:
                return(n)
            else:
                n += 1
