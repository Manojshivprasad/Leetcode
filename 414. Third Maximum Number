# Python 3

class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        if len(set(nums)) < 3:
            return(max(nums))
        else:
            l1 = sorted(set(nums),reverse=True)
            return(l1[2])
